# API base URL (leave empty for Vercel proxy; set to your Render URL if accessing directly)
VITE_API_BASE_URL=

# Google OAuth Client ID (optional - leave empty to disable Google sign-in)
VITE_GOOGLE_CLIENT_ID=
